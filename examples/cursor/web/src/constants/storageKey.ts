const PREFIX = 'BIG_MEMORY';

export const AUTH = `${PREFIX}_AUTH`;
export const CSRF = `${PREFIX}_CSRF`;
export const LANGUAGE = `${PREFIX}_LANGUAGE`;
export const COLOR_MODE = `${PREFIX}_COLOR_MODE`;
export const STORE = `${PREFIX}_STORE`;
export const REDIRECT = `${PREFIX}_REDIRECT`;
export const INVITE_CODE = `${PREFIX}_INVITE_CODE`;
