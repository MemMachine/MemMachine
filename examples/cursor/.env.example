# MemMachine Backend Configuration
MEMORY_BACKEND_URL=http://localhost:8080

# MCP Server Configuration
CURSOR_MCP_PORT=8001

# SSL Configuration
# Set to true if you need to verify SSL certificates (for production)
VERIFY_SSL=false

# Authentication Configuration (Optional)
# Uncomment and set these if you want to enable authentication
# MCP_AUTH_TOKEN=your-secret-token-here
# AUTH_USERNAME=admin
# AUTH_PASSWORD=your-secure-password

# Token TTL in seconds (default: 7 days)
TOKEN_TTL_SECONDS=604800

# HTTP Configuration
REQUEST_TIMEOUT=30
